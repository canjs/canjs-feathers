/* global describe, it, window, require */
(function () {
  var assert = window.assert;

  describe('can.Feathers.Model', function () {
    it('does something', function () {
      assert.ok(can.Feathers, 'Did something');
    });
  });
})();
